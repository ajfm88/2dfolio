import { Z } from '../settings.js';
import { tuning } from './tuning.js';
import { Collectible } from '../game/collectibles.js';
import { Spikes } from '../game/hazards/spikes.js';
import { Shooter } from '../game/hazards/shooter.js';
import { Crabby } from '../game/entities/crabby.js';
import { FierceTooth } from '../game/entities/fierce-tooth.js';
import { PinkStar } from '../game/entities/pink-star.js';

/**
 * @typedef {{
 *   id: string,
 *   group: string,
 *   label: string,
 *   icon: string,
 *   placement: 'tile' | 'entity' | 'decor' | 'marker',
 *   layer: import('../types.js').LayerName | null,
 *   z: number,
 *   clip?: string,
 *   fx?: string,
 *   coins?: number,
 *   heal?: number,
 *   spriteW?: number,
 *   spriteH?: number,
 *   hitboxW?: number,
 *   hitboxH?: number,
 *   drawOffsetX?: number,
 *   drawOffsetY?: number,
 *   flipOffsetX?: number,
 *   clips?: string,
 *   speed?: number,
 *   senseRange?: number,
 *   cooldown?: number,
 *   artFacing?: number,
 *   strikeW?: number,
 *   lungeSpeed?: number,
 *   spinTime?: number,
 *   fireFrame?: number,
 *   fireFx?: string,
 *   fireFxAnchorX?: number,
 *   fireFxAnchorY?: number,
 *   muzzleX?: number,
 *   muzzleY?: number,
 *   projectile?: import('../game/hazards/projectile.js').ProjectileSpec,
 *   defaultProps?: Record<string, unknown>,
 *   spawn?: (world: unknown, rec: import('../types.js').EntityRecord) => unknown,
 * }} PaletteEntry
 */

export const PALETTE_ORDER = [
  'terrain', 'platforms', 'water', 'treasure',
  'enemies', 'hazards', 'decor', 'markers',
];

/** @type {PaletteEntry[]} */
export const palette = [
  {
    id: 'spawn',
    group: 'markers',
    label: 'Spawn',
    icon: 'player/idle',
    placement: 'marker',
    layer: null,
    z: Z.main,
  },
  {
    id: 'goal',
    group: 'markers',
    label: 'Goal',
    icon: 'flag',
    placement: 'marker',
    layer: null,
    z: Z.main,
  },
  {
    id: 'terrain',
    group: 'terrain',
    label: 'Terrain',
    icon: 'tiles/island',
    placement: 'tile',
    layer: 'terrain',
    z: Z.bgTiles,
  },
  {
    id: 'platform',
    group: 'platforms',
    label: 'Platform',
    icon: 'tiles/island',
    placement: 'tile',
    layer: 'platform',
    z: Z.bgTiles,
  },
  {
    id: 'water',
    group: 'water',
    label: 'Water',
    icon: 'water/top',
    placement: 'tile',
    layer: 'water',
    z: Z.water,
  },
  {
    id: 'coin_gold',
    group: 'treasure',
    label: 'Gold Coin',
    icon: 'coin/gold',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'coin/gold',
    fx: 'fx/coin',
    coins: tuning.coinGold,
    // idle 16×16, art at x3 y3 w11 h11
    spriteW: 16,
    spriteH: 16,
    hitboxW: 11,
    hitboxH: 11,
    drawOffsetX: -3,
    drawOffsetY: -3,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'coin_silver',
    group: 'treasure',
    label: 'Silver Coin',
    icon: 'coin/silver',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'coin/silver',
    fx: 'fx/coin',
    coins: tuning.coinSilver,
    // idle 16×16, art at x3 y3 w11 h11
    spriteW: 16,
    spriteH: 16,
    hitboxW: 11,
    hitboxH: 11,
    drawOffsetX: -3,
    drawOffsetY: -3,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'diamond_red',
    group: 'treasure',
    label: 'Red Diamond',
    icon: 'diamond/red',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'diamond/red',
    fx: 'fx/diamond',
    coins: tuning.coinDiamond,
    // idle 24×24, art at x5 y5 w13 h13
    spriteW: 24,
    spriteH: 24,
    hitboxW: 13,
    hitboxH: 13,
    drawOffsetX: -5,
    drawOffsetY: -5,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'diamond_green',
    group: 'treasure',
    label: 'Green Diamond',
    icon: 'diamond/green',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'diamond/green',
    fx: 'fx/diamond',
    coins: tuning.coinDiamond,
    // idle 24×24, art at x5 y5 w13 h13
    spriteW: 24,
    spriteH: 24,
    hitboxW: 13,
    hitboxH: 13,
    drawOffsetX: -5,
    drawOffsetY: -5,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'diamond_blue',
    group: 'treasure',
    label: 'Blue Diamond',
    icon: 'diamond/blue',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'diamond/blue',
    fx: 'fx/diamond',
    coins: tuning.coinDiamond,
    // idle 24×24, art at x5 y5 w13 h13
    spriteW: 24,
    spriteH: 24,
    hitboxW: 13,
    hitboxH: 13,
    drawOffsetX: -5,
    drawOffsetY: -5,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'skull',
    group: 'treasure',
    label: 'Golden Skull',
    icon: 'skull/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'skull/idle',
    fx: 'fx/skull',
    coins: tuning.coinSkull,
    // idle 24×28, art at x5 y1 w16 h25
    spriteW: 24,
    spriteH: 28,
    hitboxW: 16,
    hitboxH: 25,
    drawOffsetX: -5,
    drawOffsetY: -1,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'potion_red',
    group: 'treasure',
    label: 'Red Potion',
    icon: 'potion/red',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'potion/red',
    fx: 'fx/potion',
    heal: tuning.potionHeal,
    // idle 13×17, art at x2 y3 w9 h14
    spriteW: 13,
    spriteH: 17,
    hitboxW: 9,
    hitboxH: 14,
    drawOffsetX: -2,
    drawOffsetY: -3,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'potion_blue',
    group: 'treasure',
    label: 'Blue Potion',
    icon: 'potion/blue',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'potion/blue',
    fx: 'fx/potion',
    heal: tuning.potionHeal,
    // idle 13×17, art at x3 y4 w7 h13
    spriteW: 13,
    spriteH: 17,
    hitboxW: 7,
    hitboxH: 13,
    drawOffsetX: -3,
    drawOffsetY: -4,
    spawn(world, cell) { return new Collectible(world, cell, this); },
  },
  {
    id: 'spikes',
    group: 'hazards',
    label: 'Spikes',
    icon: 'spikes',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clip: 'spikes',
    // idle 32×32, art at x0 y16 w31 h16 — hitbox full cell width, bottom 16
    spriteW: 32,
    spriteH: 32,
    hitboxW: 32,
    hitboxH: 16,
    drawOffsetX: 0,
    drawOffsetY: -16,
    spawn(world, cell) { return new Spikes(world, cell, this); },
  },
  {
    id: 'crabby',
    group: 'enemies',
    label: 'Crabby',
    icon: 'crabby/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clips: 'crabby',
    speed: 30,
    senseRange: 64,
    cooldown: 1.2,
    // Drawn face-on: never flipped.
    artFacing: 0,
    // Width of the crabby/attack-effect frame, the reach the art actually shows.
    strikeW: 118,
    // idle f0 72x32, art at x17 y6 w42 h23, feet at y29
    hitboxW: 36,
    hitboxH: 21,
    drawOffsetX: -20,
    drawOffsetY: -8,
    defaultProps: { dir: -1 },
    spawn(world, rec) { return new Crabby(world, rec, this); },
  },
  {
    id: 'fierce_tooth',
    group: 'enemies',
    label: 'Fierce Tooth',
    icon: 'tooth/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clips: 'tooth',
    speed: 45,
    senseRange: 96,
    cooldown: 1,
    // The only one drawn in profile, facing left.
    artFacing: -1,
    lungeSpeed: 170,
    // idle f0 34x30, art at x7 y5 w23 h23, feet at y28
    hitboxW: 17,
    hitboxH: 21,
    drawOffsetX: -10,
    drawOffsetY: -7,
    // Art sits 3 px right of its canvas centre, so a canvas flip would shift it
    // left by that much: drawOffsetX + (2*7 + 23 - 34).
    flipOffsetX: -7,
    defaultProps: { dir: -1 },
    spawn(world, rec) { return new FierceTooth(world, rec, this); },
  },
  {
    id: 'pink_star',
    group: 'enemies',
    label: 'Pink Star',
    icon: 'star/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clips: 'star',
    speed: 60,
    senseRange: 56,
    cooldown: 0.9,
    // Drawn face-on: never flipped.
    artFacing: 0,
    // Outlasts the 0.4 s attack clip, which wraps — a spin should loop.
    spinTime: 0.8,
    // idle f0 34x30, art at x3 y4 w27 h25, feet at y29
    hitboxW: 21,
    hitboxH: 23,
    drawOffsetX: -6,
    drawOffsetY: -6,
    defaultProps: { dir: -1 },
    spawn(world, rec) { return new PinkStar(world, rec, this); },
  },
  {
    id: 'cannon',
    group: 'hazards',
    label: 'Cannon',
    icon: 'cannon/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clips: 'cannon',
    // Under 256 px (half the narrowest viewport), so it is always on screen when
    // it fires. Faster ammo than the seashell but a longer wind-down.
    senseRange: 208,
    cooldown: 3,
    fireFrame: 3,
    // Drawn facing left; flips when dir > 0.
    artFacing: -1,
    fireFx: 'cannon/fire-effect',
    // fire-effect canvas 20x28: persistent column x≈11, frames centre y≈14.
    fireFxAnchorX: 11,
    fireFxAnchorY: 14,
    // idle f0 40x26, art at x5 y3 w30 h23, feet at y26
    hitboxW: 24,
    hitboxH: 21,
    drawOffsetX: -8,
    drawOffsetY: -5,
    // Art centred on its canvas, so the flip needs no correction.
    flipOffsetX: -8,
    // fire f3 muzzle: leftmost opaque col x0, vertical mid y11 → offset from hitbox.
    muzzleX: -8,
    muzzleY: 6,
    projectile: {
      clip: 'cannon/ball',
      // ball 16x16, art at x0 y1 w15 h15
      hitboxW: 11,
      hitboxH: 11,
      drawOffsetX: -2,
      drawOffsetY: -3,
      // Faster than the player's 100 run — you cannot out-run it.
      speed: 150,
      lifetime: 3,
      hitFx: 'cannon/ball-explode',
      endFx: 'cannon/ball-dead',
    },
    defaultProps: { dir: -1 },
    spawn(world, rec) { return new Shooter(world, rec, this); },
  },
  {
    id: 'seashell',
    group: 'hazards',
    label: 'Seashell',
    icon: 'seashell/idle',
    placement: 'entity',
    layer: null,
    z: Z.main,
    clips: 'seashell',
    senseRange: 176,
    cooldown: 2,
    fireFrame: 3,
    artFacing: -1,
    // idle f0 48x38, art at x9 y17 w32 h21, feet at y38
    hitboxW: 26,
    hitboxH: 19,
    drawOffsetX: -12,
    drawOffsetY: -19,
    // Art sits 2 px right of centre, so a canvas flip needs the correction.
    flipOffsetX: -10,
    // fire f3 muzzle: leftmost opaque col x0, vertical mid y28 → offset from hitbox.
    muzzleX: -12,
    muzzleY: 9,
    projectile: {
      clip: 'pearl/idle',
      // pearl 16x16, art at x5 y4 w7 h7
      hitboxW: 5,
      hitboxH: 5,
      drawOffsetX: -6,
      drawOffsetY: -5,
      // SPW create_pearl(150) halved: slower than the player's 100 run, so it can
      // be out-run. Lifetime is not halved — it is not a spatial quantity.
      speed: 75,
      lifetime: 5,
      hitFx: 'pearl/dead',
      endFx: 'pearl/dead',
    },
    defaultProps: { dir: -1 },
    spawn(world, rec) { return new Shooter(world, rec, this); },
  },
];

/** @type {Map<string, PaletteEntry>} */
const lookup = new Map(palette.map((e) => [e.id, e]));

/**
 * @param {string} id
 * @returns {PaletteEntry | undefined}
 */
export function byId(id) {
  return lookup.get(id);
}
